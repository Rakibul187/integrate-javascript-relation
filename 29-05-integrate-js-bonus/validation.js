const number = [1, 3, 6, 9]
console.log(Array.isArray(number));

if (Array.isArray(number) === true) {
    console.log('number is a array')
}

const money = 100;
if (typeof (money) === 'number') {
    console.log('money is a number type ariable')
}